import { AcceptedReviewPipePipe } from './accepted-review-pipe.pipe';

describe('AcceptedReviewPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptedReviewPipePipe();
    expect(pipe).toBeTruthy();
  });
});
